import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('placeholder', (WidgetTester tester) async {
    // TODO: add real tests after BLE mocking is set up
    expect(true, isTrue);
  });
}
